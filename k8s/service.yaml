apiVersion: v1
kind: Service
metadata:
  name: weather-ml-app-service
spec:
  selector:
    app: weather-ml-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30001
  type: NodePort

